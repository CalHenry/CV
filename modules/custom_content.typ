// Customizable skills keywords

// Add new lines for the 'skills' (Compétences) table
#let cloud_skills = (
  text("Cloud", weight: 600),
  "AWS, GCP, Databricks",
)

// Add to existing list
#let data_viz_extra = ("Plotly")
#let ml_models = ("XGBoost")

// Add inside list
#let python_libs = "(pandas, numpy, polars)"
